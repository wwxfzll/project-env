<template>
    <div class="<%= mDirName + '-business' %>">
        <<%= vueName %>>
        </<%= vueName %>>
    </div>
</template>

<script>
    import api from '@common/js/api'
    import <%= mName %> from '<%= '@' + mDir %>'
    <% libs.forEach(function(lib){ %>
    import <%= lib %> from '<%= lib %>'
    <% }) %>
    export default {
        components: {
            <%= mName %>
        }
    }
</script>

<style lang="scss" scoped>
    @import "~@common/scss/_common.scss";
    .<%= mDirName + '-business' %> {
    }
</style>
